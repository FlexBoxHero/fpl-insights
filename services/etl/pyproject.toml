[project]
name = "fpl-etl"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fpl-shared"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fpl_etl"]
